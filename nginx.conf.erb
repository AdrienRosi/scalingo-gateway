location /api/v1 {
  proxy_pass <%= ENV["API_V1_BACKEND"] %>;
  rewrite ^/api/v1(.*)$ / break;
}

location /api/v2 {
  proxy_pass <%= ENV["API_V2_BACKEND"] %>;
  rewrite ^/api/v2(.*)$ / break;
}

return 503;